name: ls
version: 0.0.1
author: David Moreno <dmoreno@serverboards.io>
id: serverboards.example.ls

components:
 - name: ls
   id: ls
   cmd: ./ls.py
   version: 0.0.1
   type: iocmd
   timeout: 1000 # no messages in 1s, shut it down
   singleton: true # no need to launch more than one
