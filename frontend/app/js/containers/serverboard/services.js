import React from 'react'

function Services(props){
  return (
    <div className="ui text container">
      <h1>No services yet for {props.serverboard.name}</h1>
    </div>
  )
}

export default Services
