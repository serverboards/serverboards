import React from 'react'

function Components(props){
  return (
    <div className="ui text container">
      <h1>No components yet for {props.service.name}</h1>
    </div>
  )
}

export default Components
